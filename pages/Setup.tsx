import React from 'react';

const Setup: React.FC = () => {
  return (
    <div>
      {/* This component is no longer in use as the authentication system has been removed. */}
    </div>
  );
};

export default Setup;
